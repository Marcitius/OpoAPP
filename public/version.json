{
  "version": "opogc-v9.8-respuesta-escrita-rubrica-json",
  "date": "2026-09-22",
  "features": [
    "new respuesta_escrita study type generated from ChatGPT JSON",
    "ChatGPT defines the complete scoring rubric, key concepts, weights, accepted variants and thresholds",
    "OpoGC evaluates written answers locally and returns a weighted precision percentage",
    "critical criteria can cap the maximum score when missed",
    "written precision history is stored inside the card using existing options persistence, so no D1 schema migration is required",
    "FSRS rating is assigned automatically from JSON thresholds",
    "Aprender and Mas falladas prioritize written cards using recent precision in addition to the existing memory model",
    "dedicated Respuesta escrita study button appears in folders that contain written cards",
    "existing flashcards, tests, vocabulary, orthography, local JSON storage and manual cloud backup remain unchanged"
  ]
}
